{
    "name": "Goalspeech Dev Container",
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            "USERNAME": "vscode"
            "USER_UID": "1002",
            "USER_GID": "1000"
        }
    },
    "postCreateCommand": "./setup.sh",
    "remoteUser": "vscode", // Adjust this if necessary
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.shell.linux": "/bin/bash"
            },
            "extensions": [
                "ms-python.python",
                "ms-azuretools.vscode-docker"
            ]
        }
    },
    "runArgs": [
        "--gpus", "all",
        "--ipc=host",
        "--ulimit", "memlock=-1",
        "--ulimit", "stack=67108864"
    ]
}
